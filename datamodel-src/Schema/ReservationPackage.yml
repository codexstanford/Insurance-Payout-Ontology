ReservationPackage:
  type: object
  documentation:
    en: A group of multiple reservations with common values for all
      sub-reservations.
  inherit: Reservation
  properties:
    subReservation:
      type: Reservation
      documentation:
        en: The individual reservations included in the package. Typically a repeated
          property.
