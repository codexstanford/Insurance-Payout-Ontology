PlanAction:
  type: object
  documentation:
    en: The act of planning the execution of an event/task/action/reservation/plan
      to a future date.
  inherit: OrganizeAction
  properties:
    scheduledTime:
      type: dateTime
      documentation:
        en: The time the object is scheduled to.
