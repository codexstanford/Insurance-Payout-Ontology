UserReview:
  type: object
  documentation:
    en: A review created by an end-user (e.g. consumer, purchaser, attendee etc.),
      in contrast with [[CriticReview]].
  inherit: Review
  properties: {}
