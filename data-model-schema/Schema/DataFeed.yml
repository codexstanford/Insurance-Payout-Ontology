DataFeed:
  type: object
  documentation:
    en: A single feed providing structured information about one or more entities or
      topics.
  inherit: Dataset
  properties:
    dataFeedElement:
      type:
        - Thing
        - DataFeedItem
        - string
      documentation:
        en: An item within in a data feed. Data feeds may have many elements.
