MapCategoryType:
  type: enum
  documentation:
    en: An enumeration of several kinds of Map.
  inherit: Enumeration
  values:
    SeatingMap:
      documentation:
        en: A seating map.
    VenueMap:
      documentation:
        en: A venue map (e.g. for malls, auditoriums, museums, etc.).
    ParkingMap:
      documentation:
        en: A parking map.
    TransitMap:
      documentation:
        en: A transit map.
